<Window x:Class="PCBPlotter.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" SizeToContent="Height" Width="420"
        MinHeight="160" MaxHeight="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{StaticResource BackgroundMediumBrush}"
            BorderBrush="{StaticResource BorderLightBrush}"
            BorderThickness="1" CornerRadius="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom title bar -->
            <Border Grid.Row="0" Background="{StaticResource BackgroundDarkBrush}"
                    CornerRadius="4,4,0,0" Padding="12,8"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock x:Name="TitleText" Text="Input"
                               FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Content="X" HorizontalAlignment="Right"
                            Width="24" Height="24" Padding="0"
                            Click="CloseButton_Click"
                            Background="Transparent" BorderThickness="0"/>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="PromptText" Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8"/>

                <TextBox x:Name="InputTextBox" Grid.Row="1" Padding="4"/>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="OK" Width="80" Padding="8,4" IsDefault="True" Click="OkButton_Click"/>
                    <Button Content="Cancel" Width="80" Padding="8,4" Margin="8,0,0,0" IsCancel="True"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
